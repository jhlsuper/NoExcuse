# Daily Log

## 2026-02-22

### Done
- Firebase 설정 (Android, iOS) 및 프로젝트 config 연동
- Firebase config 파일 git tracking에서 제거
- Instagram 스타일 피드 화면 구현 (mock data 기반)
- FeedCard에서 디자인 시스템 토큰 및 공유 UI 컴포넌트 분리
- `@types` path alias를 `@typeDefs`로 변경 (TS 내장 `@types`와 충돌 해결)
- App.tsx의 인라인 스타일을 StyleSheet로 리팩토링

### Notes
- "Cannot use JSX unless '--jsx' flag is provided" 에러 → tsconfig 설정은 정상, VS Code에서 TypeScript 버전을 Workspace Version으로 바꿔야 해결됨
